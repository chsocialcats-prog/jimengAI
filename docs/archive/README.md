# 归档说明

这里保存已经完成、过期或只用于一次性验收的材料，不作为当前实现的规范来源。

- `history/`：根目录历史总结和阶段性开发计划
- `handoffs/`：旧的 Agent handoff 文档
- `plans/`：已完成的 superpowers 实施计划
- `qa/`：一次性截图和 Playwright 状态文件

当前行为以代码、schema、测试和 `docs/superpowers/baselines/` 中的冻结记录为准。
