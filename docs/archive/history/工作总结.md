# AI 对话冒险平台 · 工作总结

## 1. 项目概况

这是一个个人自用、纯文字、20+ 成人向的 AI 对话冒险本地软件：

- 使用 DeepSeek 官方 API（OpenAI 兼容格式），未配置 Key 时自动进入 Mock 模式
- 无图片、无语音、无账号系统、无社区，所有数据保存在本机
- 核心玩法：作品库 → 角色卡/世界书 → 流式对话冒险 → 状态变化 → 存档读档
- 多线程协作开发：主线程负责计划和联调，W1/W2+W3/W4 是独立开发线程

## 2. 完成状态

| 工作流 | 状态 | 内容 |
| --- | --- | --- |
| W1 骨架与启动 | 完成 | 项目结构、配置、启动脚本、SQLite 初始化、空页面、接口契约 |
| W2+W3 后端引擎 | 完成 | DeepSeek 流式接入、冒险引擎、状态/骰子、存档，冒烟测试 29/29 |
| W4 前端界面 | 完成 | 作品库、详情、冒险页、创作台、设置页、离线演示数据 |
| W5 联调打磨 | 完成第一轮 | 桌面端自动流程验证通过，无 JS 报错 |

## 3. 技术架构

```text
前端：纯 HTML/CSS/JS（无构建工具，中文界面）
后端：Python FastAPI
数据库：SQLite（data/app.db）
AI：DeepSeek OpenAI 兼容流式接口；无 Key 时用本地 mock 客户端
```

### 后端模块

```text
backend/
├── ai/deepseek_client.py   # DeepSeek 流式客户端与 mock 客户端
├── config.py               # 读取 config.json，支持环境变量
├── database.py             # SQLite 自动建表
├── repositories.py         # 数据读写
├── routers/                # 配置、作品、角色卡、世界书、会话、对话、导入
├── services/
│   ├── adventure_engine.py # 上下文组装、世界书、记忆摘要、结构化输出
│   ├── commands.py         # 快捷指令（中文 + 英文）
│   ├── roll_service.py     # 骰子与判定
│   ├── snapshot_service.py # 手动/自动存档、读档
│   ├── state_service.py    # 状态合并与持久化
│   └── card_importer.py    # 文本角色卡解析导入
├── main.py                 # FastAPI 入口
└── smoke_test.py           # 后端冒烟测试
```

## 4. 已实现功能

### 作品库

- 作品卡片列表、搜索、标签筛选、推荐/最新/热门排序
- 空状态时一键载入示例作品

### 作品详情

- 作品简介、标签、开场剧情
- 角色卡：人设、性格、语气、固定指令、初始属性、关系
- 世界书：条目、关键词、优先级

### 冒险页

- 流式对话（SSE + 打字机效果）
- 自由输入 + 剧情选项按钮
- 快捷指令：`/掷骰 2d6+2`、`/状态`、`/背包`、`/存档`、`/帮助`
- 英文别名仍兼容：`/roll`、`/status`、`/inventory`、`/save`、`/help`
- 其他别名：`/判定`、`/自动存档`、`/读档`
- 右侧状态面板：属性、金钱、物品、任务、关系、日志
- 多存档位、自动存档、手动存档、读档、删除
- 停止当前回复、删除会话

### 创作台

- 可视化创建角色卡、世界书、作品
- JSON 角色卡导入
- 文本角色卡粘贴导入：自动解析「正文 / 环境 / 状态栏 / 记忆区 / 偷听心声」并创建完整作品

### 设置

- DeepSeek Base URL、API Key、模型、温度、最大回复长度
- 一键测试连接

### 其他

- 首次启动 20+ 年龄确认
- 暗色/明亮主题切换
- Mock 模式 / DeepSeek 在线 / 离线演示三种状态

## 5. 示例作品

已导入作品库：

1. 《雾中王都》（示例，奇幻大世界）
2. 《包养合约（沈知夏）》（24 岁成年角色，现代包养题材）
3. 《同居试爱（江漓）》（27 岁成年角色，现代都市）
4. 《深夜画室（温挽）》（26 岁成年角色，现代都市）

文本卡源文件位于 `data/cards/`，批量导入脚本为 `tools/import_cards.py`。

## 6. 验证结果

- 后端冒烟测试：29/29 通过
  - 健康检查、配置、角色卡/世界书/作品 CRUD
  - 会话创建、Mock 流式对话、结构化状态变化
  - `/掷骰` 中文指令、自动攻击判定
  - 手动存档、修改状态、读档恢复
- 前端：语法检查通过
- 桌面端自动流程验证通过：作品库 → 详情 → 开始冒险 → 流式对话，无 JS 报错
- 移动端：按用户要求未验证

## 7. 使用方法

```powershell
python start.py
```

或双击 `start.bat`，然后访问：

```text
http://127.0.0.1:8000
```

接 DeepSeek 真 AI：

1. 打开网页「设置」，填入 API Key 保存
2. 或编辑 `config.json` 的 `deepseek.api_key`
3. 顶部状态变为「DeepSeek 在线」后即可

## 8. 后续建议

- 填入真实 DeepSeek Key，实测成人向内容的模型表现与审查边界
- 继续导入更多文本卡，丰富作品库
- 优化记忆摘要和世界书关键词触发
- 打包成独立 `.exe`
- 移动端适配验证
- 多会话管理、分支时间线可视化
