# 开发状态

截至 2026-08-14，当前工作聚焦于将既有 AI 文字冒险后端接入新的静态导出 Next 前端，并补齐用户与角色卡头像能力：未提交的后端改动已把用户和 `cards` 的 `avatar_url` 纳入 SQLite 迁移与认证/角色卡 API，新增仅允许登录用户上传 PNG、JPEG、WebP、GIF（最大 5MB）到已忽略的 `data/uploads/`，并由 `backend/main.py` 暴露 `/uploads`；启动时若 `ai/ai/out/index.html` 存在则优先托管该 Next 导出，否则回退到既有 `frontend/`，默认生成配置也已调整为 `max_tokens=4096`、`reasoning_effort=high`。核心变更位于 `backend/main.py`、`backend/migrations/account_schema.py`、`backend/auth/`、`backend/repository/cards.py`、`backend/routers/uploads_routes.py`、`backend/services/image_uploads.py` 和未跟踪的 `ai/ai/`；`ai/ai/` 包含源码、静态 `out/` 和资源，但尚未纳入 Git，且 Next 配置仍启用 `typescript.ignoreBuildErrors`。已确认 `python -m unittest discover -s backend -p 'test_*.py'` 通过 244 项、现有 `frontend/test_*.mjs` 通过 168 项；前者有 FastAPI/Starlette 在 Python 3.14 的弃用告警及少量 HTTP/SQLite 资源警告，后者只验证旧前端，未验证 Next 构建或浏览器联调。未发现可确认的失败方案或回滚记录；下一步应先明确并提交 `ai/ai/` 源码与静态产物的版本管理策略，移除或处理忽略 TypeScript 构建错误的设置，补充新界面对头像上传和后端 API 的自动化/浏览器验证，再进行完整构建与烟测。
